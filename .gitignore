# Local Netlify folder
.netlify

# Local secrets for `netlify dev` (ElevenLabs key, etc.) — never commit
.env

# OS / editor cruft
.DS_Store

# Python / audit run artifacts
scripts/__pycache__/
scripts/*.log

# Google Places API key (never commit secrets)
scripts/.google_key
google_report.json
scripts/.yelp_key
yelp_report.json
scripts/.fsq_key
fsq_report.json
